rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /app_sync/{document=**} {
      allow read: if true;
      allow write: if false; // Only admin backend can write
    }
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
